<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 设置按钮的左上角和右下角为弧形 -->
    <!--    <stroke-->
    <!--        android:width="1dp"-->
    <!--        android:color="@color/buttonRed" />-->
    <stroke
        android:width="0.5dp"
        android:color="@color/editSolid" />
    <corners android:radius="0dp" />
</shape>